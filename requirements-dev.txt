# Development and CI dependencies. Not installed on the HA instance.
pytest>=8.0
yamllint>=1.35
# export_ha.py imports yaml directly rather than relying on yamllint pulling it in
PyYAML>=6.0

# probe_stt.py talks to Meta's realtime ASR over a websocket.
websockets>=13
# Deliberately NOT listed as a requirement: pymicro-vad. It is what Home
# Assistant itself pins, and installing it gives probe_stt.py byte-exact
# parity with HA's VAD decision — but it ships no wheels and needs a C++
# toolchain, so it cannot be a hard dependency of this repo's tooling on
# Windows. Without it the probe falls back to an energy gate and says so in
# every report it prints.
